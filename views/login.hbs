<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Album Review Login</title>
  
  <!-- Bootstrap CSS from CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    html,
    body {
      min-height: 100%;
    }
    
    body {
      display: flex;
      align-items: flex-start;
      padding-top: 100px;
      padding-bottom: 40px;
      background-color: #d8c1eb;
    }
    
    .form-signin {
      max-width: 480px;
      padding: 30px;
      background-color: white;
      border-radius: 12px;
    }
    
    .form-signin .form-floating:focus-within {
      z-index: 2;
    }
    
    .form-signin input[type="text"],
    .form-signin input[type="password"] {
      border: 2px solid #ced4da;
      margin-bottom: 15px;
      border-radius: 6px;
    }
    
    .form-heading {
      color: #5d3b8f;
      font-weight: 600;
      margin-bottom: 25px;
      font-size: 1.5rem;
    }
    
    .btn-primary {
      background-color: #8a56d1;
      border-color: #7847bd;
      font-weight: 500;
      padding: 12px;
      border-radius: 6px;
      width: 60%;
    }
    
    .btn-primary:hover {
      background-color: #6b3cb0;
      border-color: #7445b9;
    }
    
    .btn-outline-secondary {
      border-color: #8a56d1;
      color: #8a56d1;
      font-weight: 500;
      padding: 8px 20px;
      border-radius: 6px;
    }
    
    .btn-outline-secondary:hover {
      background-color: #8a56d1;
      border-color: #8a56d1;
      color: white;
    }
  </style>
</head>
<body class="text-center">
  <main class="form-signin w-100 m-auto">
    {{#if error}}
      <div class="alert alert-danger">{{error}}</div>
    {{/if}}
    
    <form action="/login" method="POST">
      <br>
      <h1 class="form-heading">Please Login</h1>
      
      <div class="form-floating mb-3">
        <input type="text" class="form-control" id="username" name="username" placeholder="Username" autocomplete="off">
        <label for="username">Username</label>
      </div>
      
      <div class="form-floating mb-3">
        <input type="password" class="form-control" id="password" name="password" placeholder="Password" autocomplete="off">
        <label for="password">Password</label>
      </div>
      
      <button class="btn btn-primary w-60 py-2 mb-3" type="submit">Sign In</button>
      
      <p class="mt-3">Don't have an account?</p>
      <a href="/" class="btn btn-outline-secondary">Register</a>
      
      <p class="mt-5 mb-3 text-body-secondary">&copy; 2025 Album Reviews</p>
    </form>
  </main>
</body>
</html>